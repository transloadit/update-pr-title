{
  "name": "transloadit-update-title",
  "description": "Update PR title for Transloadit",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js --out dist",
    "lint": "eslint index.js && yarn lint:formatting",
    "test": "eslint index.js && jest",
    "fix:formatting": "prettier --write .",
    "lint:formatting": "prettier --check .",
    "fix": "yarn fix:formatting"
  },
  "author": "Transloadit",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^4.0.0",
    "@slack/webhook": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^6.3.0",
    "eslint-config-prettier": "^8.8.0",
    "jest": "^24.9.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.6"
  }
}
